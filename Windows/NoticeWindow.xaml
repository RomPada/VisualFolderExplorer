<Window x:Class="VisualFolderExplorer.Windows.NoticeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="500" SizeToContent="Height" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{StaticResource WindowBg}">
    <Border Style="{StaticResource CardBorder}" Margin="12" Padding="20">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="20"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="14"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <Border x:Name="IconCircle" Width="40" Height="40" CornerRadius="20" Background="#E8F2FB">
                    <TextBlock x:Name="IconText" Text="i" FontSize="20" FontWeight="Bold" Foreground="#3D74A8" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock x:Name="MessageText" Grid.Column="2" MaxWidth="400" TextWrapping="Wrap" FontSize="14" VerticalAlignment="Center" Foreground="{StaticResource TextBrush}"/>
            </Grid>
            <Button x:Name="OkButton" Grid.Row="2" HorizontalAlignment="Right" MinWidth="100" Style="{StaticResource PrimaryButton}" IsDefault="True" Click="OkButton_Click"/>
        </Grid>
    </Border>
</Window>

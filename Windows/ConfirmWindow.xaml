<Window x:Class="VisualFolderExplorer.Windows.ConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="520" SizeToContent="Height" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{StaticResource WindowBg}">
    <Border Style="{StaticResource CardBorder}" Margin="12" Padding="20">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="20"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="14"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <Border Width="40" Height="40" CornerRadius="20" Background="#FFF3D9">
                    <TextBlock Text="!" FontSize="22" FontWeight="Bold" Foreground="#9A6A14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock x:Name="MessageText" Grid.Column="2" MaxWidth="420" TextWrapping="Wrap" FontSize="14" VerticalAlignment="Center" Foreground="{StaticResource TextBrush}"/>
            </Grid>
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelButton" MinWidth="100" Margin="0,0,8,0" IsCancel="True" Click="CancelButton_Click"/>
                <Button x:Name="SecondaryButton" MinWidth="110" Margin="0,0,8,0" Click="SecondaryButton_Click"/>
                <Button x:Name="PrimaryButton" MinWidth="100" Style="{StaticResource PrimaryButton}" IsDefault="True" Click="PrimaryButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
